# Copy these into Netlify > Site settings > Build & deploy > Environment
# For Vite, ONLY variables starting with VITE_ are exposed to the frontend:

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Add any other VITE_ variables you need here
