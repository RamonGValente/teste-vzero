# Netlify config (NPM + Vite React)
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback (Vite)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
