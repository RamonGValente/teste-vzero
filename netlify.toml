# Netlify config (Bun + Vite React)
[build]
  command = "bun install && bun run build"
  publish = "dist"

[build.environment]
  BUN_VERSION = "1.1.30"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
