*** Begin Patch
*** Update File: src/components/social/SocialContactsList.tsx
@@
-import React, { useMemo } from 'react';
+import React, { useMemo } from 'react';
+import { usePresenceForContacts } from '@/hooks/usePresenceForContacts'; // <-- adicionado
+import { isOnline } from '@/lib/presence';                                // <-- adicionado
@@
-  const contacts = useMemo(() => contactProfiles ?? [], [contactProfiles]);
+  const contacts = useMemo(() => contactProfiles ?? [], [contactProfiles]);
+  const contactIds = useMemo(() => contacts.map((c: any) => c.id), [contacts]);
+  const presenceMap = usePresenceForContacts(contactIds); // polling 30s + realtime
@@
-  return (
+  return (
     <div>
       {contacts.map((c: any) => {
-        const status = c.status ?? 'offline';
-        const lastSeen = c.last_seen ?? null;
-        const online = status === 'online';
+        const p = presenceMap[c.id];
+        const status = (p?.status ?? c.status ?? 'offline') as string;
+        const lastSeen = p?.last_seen ?? c.last_seen ?? null;
+        const online = isOnline(status, lastSeen); // calcula corretamente
         return (
           <ContactListItem
             key={c.id}
             contact={c}
-            presence={{ status, last_seen: lastSeen }}
+            presence={{ status, last_seen: lastSeen }}
             online={online}
           />
         );
       })}
     </div>
   );
*** End Patch
